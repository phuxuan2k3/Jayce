import BackgroundPracticeGen from "./practice-gen/BackgroundPracticeGen";

/**
 * @returns JSX.Element
 * @description This component is a placeholder for background jobs functionality. Prevents the user from navigating away while jobs are running.
 */
export default function BackgroundJobs() {
	return <>
		<BackgroundPracticeGen />
	</>;
}
